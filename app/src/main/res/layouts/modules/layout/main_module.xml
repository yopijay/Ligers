<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:smb="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/backgroundDefault">

    <include
        layout="@layout/header"
        android:id="@+id/header"/>

    <View
        android:id="@+id/header_divider"
        android:layout_width="match_parent"
        android:layout_height="1dp"
        android:background="@color/dividerDefault"
        android:layout_below="@+id/header"/>

    <FrameLayout
        android:id="@+id/pageContainer"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_below="@+id/header_divider"/>

    <View
        android:id="@+id/footer_divider"
        android:layout_width="match_parent"
        android:layout_height="1dp"
        android:background="@color/dividerDefault"
        android:layout_above="@id/bottom_bar"/>

    <me.ibrahimsn.lib.SmoothBottomBar
        android:id="@+id/bottom_bar"
        android:layout_width="match_parent"
        android:layout_height="60dp"
        android:layout_alignParentBottom="true"
        smb:backgroundColor="@color/backgroundWhite"
        smb:iconSize="30dp"
        smb:iconTint="@color/textLight"
        smb:iconTintActive="@color/textLink"
        smb:menu="@menu/bottom_nav"/>

</RelativeLayout>